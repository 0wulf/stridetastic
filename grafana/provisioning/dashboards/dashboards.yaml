apiVersion: 1
providers:
  - name: 'Node telemetry dashboards'
    orgId: 1
    folder: ''
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
      # include any .json dashboards placed in grafana/dashboards
